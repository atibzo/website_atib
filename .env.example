# RSVP endpoint — your Google Apps Script Web App "/exec" URL.
# Create it by following the "RSVP → Google Sheet" section in README.md,
# then set this value here (for local dev) and as an environment variable
# in your Vercel project settings (Production + Preview).
NEXT_PUBLIC_RSVP_ENDPOINT=
